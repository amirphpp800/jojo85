{
  "name": "wireguard-bot",
  "version": "1.0.0",
  "description": "WireGuard configuration bot for Telegram with Cloudflare Pages",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --kv DB --binding BOT_TOKEN=test ADMIN_ID=123456",
    "deploy": "wrangler pages deploy .",
    "tail": "wrangler pages deployment tail"
  },
  "keywords": [
    "wireguard",
    "telegram",
    "cloudflare",
    "vpn"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
